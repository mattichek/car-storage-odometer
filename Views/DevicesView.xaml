<UserControl x:Class="car_storage_odometer.Views.DevicesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:car_storage_odometer.Views"
             xmlns:prism="http://prismlibrary.com/"
             prism:ViewModelLocator.AutoWireViewModel="True"
             Width="1282.96666666667" Height="850">

    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/car-storage-odometer;component/Styles/DarkThemeStyles.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="{StaticResource SecondaryBackgroundBrush}" CornerRadius="5" Padding="16" Margin="0,0,0,20">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Column="0" Text="Zarządzanie urządzeniami" Style="{StaticResource TitleTextBlockStyle}" FontWeight="SemiBold" VerticalAlignment="Center"/>
                <Button Grid.Column="1" Content="Dodaj nowe urządzenie"
                        Command="{Binding AddNewDeviceCommand}"
                        Style="{StaticResource {x:Type Button}}"
                        Padding="10,5"
                        Margin="0,0,0,0"/>
            </Grid>
        </Border>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="3*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="1*"/>
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0"
                    Background="{StaticResource SecondaryBackgroundBrush}"
                    BorderBrush="{StaticResource BorderBrush}"
                    BorderThickness="1"
                    CornerRadius="5"
                    Padding="16,0,16,16"
                    Margin="0,0,20,0">
                <StackPanel Orientation="Vertical">
                    <TextBlock Text="Lista urządzeń" Style="{StaticResource SubTitleTextBlockStyle}" FontWeight="SemiBold" Margin="0,16,0,10"/>
                    <Separator Background="{StaticResource BorderBrush}" Height="1" Margin="0,0,0,15"/>

                    <WrapPanel Orientation="Horizontal" HorizontalAlignment="Left" VerticalAlignment="Center" Margin="0,0,0,10">
                        <StackPanel Orientation="Horizontal" Margin="0,0,20,10">
                            <TextBlock Text="Numer ser.:" VerticalAlignment="Center" Margin="0,0,10,0" Style="{StaticResource BaseTextBlockStyle}"/>
                            <ComboBox ItemsSource="{Binding AvailableDeviceSerials}"
                                      SelectedItem="{Binding SelectedDeviceSerialFilter}"
                                      Width="120">
                                <ComboBox.ItemContainerStyle>
                                    <Style TargetType="ComboBoxItem" BasedOn="{StaticResource {x:Type ComboBoxItem}}"/>
                                </ComboBox.ItemContainerStyle>
                                <ComboBox.Template>
                                    <ControlTemplate TargetType="ComboBox">
                                        <Grid>
                                            <ToggleButton x:Name="PART_ToggleButton" Grid.Column="2"
                                                          Focusable="false"
                                                          Background="{TemplateBinding Background}"
                                                          BorderBrush="{TemplateBinding BorderBrush}"
                                                          BorderThickness="{TemplateBinding BorderThickness}">
                                                <ToggleButton.Template>
                                                    <ControlTemplate>
                                                        <Grid>
                                                            <Grid.ColumnDefinitions>
                                                                <ColumnDefinition />
                                                                <ColumnDefinition Width="Auto" />
                                                            </Grid.ColumnDefinitions>
                                                            <Border x:Name="border" Grid.ColumnSpan="2"
                                                                    Background="{TemplateBinding Background}"
                                                                    BorderBrush="{TemplateBinding BorderBrush}"
                                                                    BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="3" />
                                                            <Path x:Name="arrow" Grid.Column="1"
                                                                  Fill="{StaticResource PrimaryForegroundBrush}"
                                                                  HorizontalAlignment="Center"
                                                                  VerticalAlignment="Center"
                                                                  Data="M 0 0 L 4 4 L 8 0 Z"
                                                                  Margin="0,0,5,0" />
                                                        </Grid>
                                                        <ControlTemplate.Triggers>
                                                            <Trigger Property="IsMouseOver" Value="True">
                                                                <Setter TargetName="border" Property="Background" Value="{StaticResource HoverBackgroundBrush}" />
                                                            </Trigger>
                                                        </ControlTemplate.Triggers>
                                                    </ControlTemplate>
                                                </ToggleButton.Template>
                                            </ToggleButton>
                                            <ContentPresenter ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                                                              ContentTemplateSelector="{TemplateBinding ItemTemplateSelector}"
                                                              Content="{TemplateBinding SelectionBoxItem}"
                                                              ContentStringFormat="{TemplateBinding SelectionBoxItemStringFormat}"
                                                              HorizontalAlignment="Left"
                                                              VerticalAlignment="Center"
                                                              Margin="{TemplateBinding Padding}"/>
                                            <Popup x:Name="PART_Popup"
                                                   AllowsTransparency="True"
                                                   Placement="Bottom"
                                                   IsOpen="{TemplateBinding IsDropDownOpen}"
                                                   StaysOpen="False"
                                                   Focusable="False">
                                                <Border x:Name="DropDownBorder"
                                                        Background="{StaticResource SecondaryBackgroundBrush}"
                                                        BorderBrush="{StaticResource BorderBrush}"
                                                        BorderThickness="1"
                                                        CornerRadius="3">
                                                    <ScrollViewer Margin="4,6,4,6" MaxHeight="{TemplateBinding MaxDropDownHeight}" SnapsToDevicePixels="True">
                                                        <StackPanel IsItemsHost="True" KeyboardNavigation.DirectionalNavigation="Contained" />
                                                    </ScrollViewer>
                                                </Border>
                                            </Popup>
                                        </Grid>
                                    </ControlTemplate>
                                </ComboBox.Template>
                            </ComboBox>
                        </StackPanel>

                        <StackPanel Orientation="Horizontal" Margin="0,0,20,10">
                            <TextBlock Text="Typ:" VerticalAlignment="Center" Margin="0,0,10,0" Style="{StaticResource BaseTextBlockStyle}"/>
                            <ComboBox ItemsSource="{Binding AvailableDeviceTypes}"
                                      SelectedItem="{Binding SelectedDeviceTypeFilter}"
                                      Width="120"
                                      DisplayMemberPath="TypeName">
                                <ComboBox.ItemContainerStyle>
                                    <Style TargetType="ComboBoxItem" BasedOn="{StaticResource {x:Type ComboBoxItem}}"/>
                                </ComboBox.ItemContainerStyle>
                                <ComboBox.Template>
                                    <ControlTemplate TargetType="ComboBox">
                                        <Grid>
                                            <ToggleButton x:Name="PART_ToggleButton" Grid.Column="2"
                                                          Focusable="false"
                                                          Background="{TemplateBinding Background}"
                                                          BorderBrush="{TemplateBinding BorderBrush}"
                                                          BorderThickness="{TemplateBinding BorderThickness}">
                                                <ToggleButton.Template>
                                                    <ControlTemplate>
                                                        <Grid>
                                                            <Grid.ColumnDefinitions>
                                                                <ColumnDefinition />
                                                                <ColumnDefinition Width="Auto" />
                                                            </Grid.ColumnDefinitions>
                                                            <Border x:Name="border" Grid.ColumnSpan="2"
                                                                    Background="{TemplateBinding Background}"
                                                                    BorderBrush="{TemplateBinding BorderBrush}"
                                                                    BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="3" />
                                                            <Path x:Name="arrow" Grid.Column="1"
                                                                  Fill="{StaticResource PrimaryForegroundBrush}"
                                                                  HorizontalAlignment="Center"
                                                                  VerticalAlignment="Center"
                                                                  Data="M 0 0 L 4 4 L 8 0 Z"
                                                                  Margin="0,0,5,0" />
                                                        </Grid>
                                                        <ControlTemplate.Triggers>
                                                            <Trigger Property="IsMouseOver" Value="True">
                                                                <Setter TargetName="border" Property="Background" Value="{StaticResource HoverBackgroundBrush}" />
                                                            </Trigger>
                                                        </ControlTemplate.Triggers>
                                                    </ControlTemplate>
                                                </ToggleButton.Template>
                                            </ToggleButton>
                                            <ContentPresenter ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                                                              ContentTemplateSelector="{TemplateBinding ItemTemplateSelector}"
                                                              Content="{TemplateBinding SelectionBoxItem}"
                                                              ContentStringFormat="{TemplateBinding SelectionBoxItemStringFormat}"
                                                              HorizontalAlignment="Left"
                                                              VerticalAlignment="Center"
                                                              Margin="{TemplateBinding Padding}"/>
                                            <Popup x:Name="PART_Popup"
                                                   AllowsTransparency="True"
                                                   Placement="Bottom"
                                                   IsOpen="{TemplateBinding IsDropDownOpen}"
                                                   StaysOpen="False"
                                                   Focusable="False">
                                                <Border x:Name="DropDownBorder"
                                                        Background="{StaticResource SecondaryBackgroundBrush}"
                                                        BorderBrush="{StaticResource BorderBrush}"
                                                        BorderThickness="1"
                                                        CornerRadius="3">
                                                    <ScrollViewer Margin="4,6,4,6" MaxHeight="{TemplateBinding MaxDropDownHeight}" SnapsToDevicePixels="True">
                                                        <StackPanel IsItemsHost="True" KeyboardNavigation.DirectionalNavigation="Contained" />
                                                    </ScrollViewer>
                                                </Border>
                                            </Popup>
                                        </Grid>
                                    </ControlTemplate>
                                </ComboBox.Template>
                            </ComboBox>
                        </StackPanel>

                        <StackPanel Orientation="Horizontal" Margin="0,0,20,10">
                            <TextBlock Text="Status:" VerticalAlignment="Center" Margin="0,0,10,0" Style="{StaticResource BaseTextBlockStyle}"/>
                            <ComboBox ItemsSource="{Binding AvailableDeviceStatuses}"
                                      SelectedItem="{Binding SelectedDeviceStatusFilter}"
                                      Width="120"
                                      DisplayMemberPath="StatusName">
                                <ComboBox.ItemContainerStyle>
                                    <Style TargetType="ComboBoxItem" BasedOn="{StaticResource {x:Type ComboBoxItem}}"/>
                                </ComboBox.ItemContainerStyle>
                                <ComboBox.Template>
                                    <ControlTemplate TargetType="ComboBox">
                                        <Grid>
                                            <ToggleButton x:Name="PART_ToggleButton" Grid.Column="2"
                                                          Focusable="false"
                                                          Background="{TemplateBinding Background}"
                                                          BorderBrush="{TemplateBinding BorderBrush}"
                                                          BorderThickness="{TemplateBinding BorderThickness}">
                                                <ToggleButton.Template>
                                                    <ControlTemplate>
                                                        <Grid>
                                                            <Grid.ColumnDefinitions>
                                                                <ColumnDefinition />
                                                                <ColumnDefinition Width="Auto" />
                                                            </Grid.ColumnDefinitions>
                                                            <Border x:Name="border" Grid.ColumnSpan="2"
                                                                    Background="{TemplateBinding Background}"
                                                                    BorderBrush="{TemplateBinding BorderBrush}"
                                                                    BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="3" />
                                                            <Path x:Name="arrow" Grid.Column="1"
                                                                  Fill="{StaticResource PrimaryForegroundBrush}"
                                                                  HorizontalAlignment="Center"
                                                                  VerticalAlignment="Center"
                                                                  Data="M 0 0 L 4 4 L 8 0 Z"
                                                                  Margin="0,0,5,0" />
                                                        </Grid>
                                                        <ControlTemplate.Triggers>
                                                            <Trigger Property="IsMouseOver" Value="True">
                                                                <Setter TargetName="border" Property="Background" Value="{StaticResource HoverBackgroundBrush}" />
                                                            </Trigger>
                                                        </ControlTemplate.Triggers>
                                                    </ControlTemplate>
                                                </ToggleButton.Template>
                                            </ToggleButton>
                                            <ContentPresenter ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                                                              ContentTemplateSelector="{TemplateBinding ItemTemplateSelector}"
                                                              Content="{TemplateBinding SelectionBoxItem}"
                                                              ContentStringFormat="{TemplateBinding SelectionBoxItemStringFormat}"
                                                              HorizontalAlignment="Left"
                                                              VerticalAlignment="Center"
                                                              Margin="{TemplateBinding Padding}"/>
                                            <Popup x:Name="PART_Popup"
                                                   AllowsTransparency="True"
                                                   Placement="Bottom"
                                                   IsOpen="{TemplateBinding IsDropDownOpen}"
                                                   StaysOpen="False"
                                                   Focusable="False">
                                                <Border x:Name="DropDownBorder"
                                                        Background="{StaticResource SecondaryBackgroundBrush}"
                                                        BorderBrush="{StaticResource BorderBrush}"
                                                        BorderThickness="1"
                                                        CornerRadius="3">
                                                    <ScrollViewer Margin="4,6,4,6" MaxHeight="{TemplateBinding MaxDropDownHeight}" SnapsToDevicePixels="True">
                                                        <StackPanel IsItemsHost="True" KeyboardNavigation.DirectionalNavigation="Contained" />
                                                    </ScrollViewer>
                                                </Border>
                                            </Popup>
                                        </Grid>
                                    </ControlTemplate>
                                </ComboBox.Template>
                            </ComboBox>
                        </StackPanel>

                        <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                            <Button Content="Filtruj"
                                    Command="{Binding FilterDevicesCommand}"
                                    Style="{StaticResource {x:Type Button}}"
                                    Margin="0,0,10,0"/>
                            <Button Content="Resetuj"
                                    Command="{Binding ResetFiltersCommand}">
                                <Button.Template>
                                    <ControlTemplate TargetType="{x:Type Button}">
                                        <Border Background="#6C757D"
                                                BorderBrush="#6C757D"
                                                BorderThickness="{TemplateBinding BorderThickness}"
                                                CornerRadius="3">
                                            <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                                              VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                                              Margin="{TemplateBinding Padding}"
                                                              Content="{TemplateBinding Content}"/>
                                        </Border>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter Property="Background" Value="#5a6268"/>
                                                <Setter Property="BorderBrush" Value="#5a6268"/>
                                            </Trigger>
                                            <Trigger Property="IsPressed" Value="True">
                                                <Setter Property="Background" Value="#5a6268"/>
                                                <Setter Property="BorderBrush" Value="#5a6268"/>
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </Button.Template>
                            </Button>
                        </StackPanel>
                    </WrapPanel>

                    <DataGrid ItemsSource="{Binding FilteredDevices}"
                              SelectedItem="{Binding SelectedDevice}"
                              AutoGenerateColumns="False"
                              IsReadOnly="True"
                              Background="{StaticResource SecondaryBackgroundBrush}"
                              Foreground="{StaticResource PrimaryForegroundBrush}"
                              HeadersVisibility="Column"
                              BorderBrush="{StaticResource BorderBrush}"
                              BorderThickness="0,1,0,0"
                              GridLinesVisibility="Horizontal"
                              HorizontalGridLinesBrush="{StaticResource BorderBrush}"
                              VerticalGridLinesBrush="{StaticResource BorderBrush}"
                              Margin="0,0,0,0">
                        <DataGrid.Resources>
                            <Style TargetType="DataGridColumnHeader" BasedOn="{StaticResource DataGridColumnHeaderStyle}"/>
                            <Style TargetType="DataGridCell" BasedOn="{StaticResource DataGridCellStyle}"/>
                            <Style TargetType="DataGridRow" BasedOn="{StaticResource DataGridRowStyle}"/>
                        </DataGrid.Resources>
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Numer seryjny" Binding="{Binding SerialNumber}" Width="1.5*"/>
                            <DataGridTextColumn Header="Typ" Binding="{Binding TypeName}" Width="1*"/>
                            <DataGridTextColumn Header="Status" Binding="{Binding StatusName}" Width="1*"/>
                            <DataGridTextColumn Header="Magazyn" Binding="{Binding WarehouseName}" Width="1.2*"/>
                            <DataGridTextColumn Header="Dodane przez" Binding="{Binding AddedByUserName}" Width="1.2*"/>
                            <DataGridTemplateColumn Header="Akcje" Width="Auto">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <StackPanel Orientation="Horizontal">
                                            <Button Content="Edytuj"
                                                    Command="{Binding DataContext.EditDeviceCommand, RelativeSource={RelativeSource AncestorType={x:Type DataGrid}}}"
                                                    CommandParameter="{Binding}"
                                                    Style="{StaticResource {x:Type Button}}"
                                                    Margin="0,0,5,0"
                                                    Padding="5,2"/>
                                            <Button Content="Usuń"
                                                    Command="{Binding DataContext.DeleteDeviceCommand, RelativeSource={RelativeSource AncestorType={x:Type DataGrid}}}"
                                                    CommandParameter="{Binding}"
                                                    Background="#DC3545" BorderBrush="#DC3545"
                                                    Padding="5,2">
                                                <Button.Template>
                                                    <ControlTemplate TargetType="{x:Type Button}">
                                                        <Border Background="{TemplateBinding Background}"
                                                                BorderBrush="{TemplateBinding BorderBrush}"
                                                                BorderThickness="{TemplateBinding BorderThickness}"
                                                                CornerRadius="3">
                                                            <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                                                              VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                                                              Margin="{TemplateBinding Padding}"
                                                                              Content="{TemplateBinding Content}"/>
                                                        </Border>
                                                        <ControlTemplate.Triggers>
                                                            <Trigger Property="IsMouseOver" Value="True">
                                                                <Setter Property="Background" Value="#c82333"/>
                                                                <Setter Property="BorderBrush" Value="#c82333"/>
                                                            </Trigger>
                                                            <Trigger Property="IsPressed" Value="True">
                                                                <Setter Property="Background" Value="#c82333"/>
                                                                <Setter Property="BorderBrush" Value="#c82333"/>
                                                            </Trigger>
                                                        </ControlTemplate.Triggers>
                                                    </ControlTemplate>
                                                </Button.Template>
                                            </Button>
                                        </StackPanel>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                        </DataGrid.Columns>
                    </DataGrid>
                </StackPanel>
            </Border>

            <GridSplitter Grid.Column="1" Width="5" HorizontalAlignment="Center" VerticalAlignment="Stretch" Background="{StaticResource BorderBrush}" ResizeBehavior="PreviousAndNext"/>

            <Border Grid.Column="2"
                    Background="{StaticResource SecondaryBackgroundBrush}"
                    BorderBrush="{StaticResource BorderBrush}"
                    BorderThickness="1"
                    CornerRadius="5"
                    Padding="16">
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <StackPanel Orientation="Vertical">
                        <TextBlock Text="{Binding CurrentOperationText}" Style="{StaticResource SubTitleTextBlockStyle}" FontWeight="SemiBold" Margin="0,0,0,10"/>
                        <Separator Background="{StaticResource BorderBrush}" Height="1" Margin="0,0,0,15"/>

                        <StackPanel Margin="0,0,0,10">
                            <TextBlock Text="Numer seryjny:" Style="{StaticResource BaseTextBlockStyle}" Margin="0,0,0,5"/>
                            <TextBox Text="{Binding EditedDevice.SerialNumber, UpdateSourceTrigger=PropertyChanged}"
                                     Style="{StaticResource {x:Type TextBox}}"
                                     IsEnabled="{Binding IsEditingNewDevice}"/>
                        </StackPanel>
                        <StackPanel Margin="0,0,0,10">
                            <TextBlock Text="Typ urządzenia:" Style="{StaticResource BaseTextBlockStyle}" Margin="0,0,0,5"/>
                            <ComboBox ItemsSource="{Binding AvailableDeviceTypes}"
                                      SelectedItem="{Binding EditedDeviceType, Mode=TwoWay}"
                                      DisplayMemberPath="TypeName"
                                      Style="{StaticResource {x:Type ComboBox}}"/>
                        </StackPanel>
                        <StackPanel Margin="0,0,0,10">
                            <TextBlock Text="Status urządzenia:" Style="{StaticResource BaseTextBlockStyle}" Margin="0,0,0,5"/>
                            <ComboBox ItemsSource="{Binding AvailableDeviceStatuses}"
                                      SelectedItem="{Binding EditedDeviceStatus, Mode=TwoWay}"
                                      DisplayMemberPath="StatusName"
                                      Style="{StaticResource {x:Type ComboBox}}"/>
                        </StackPanel>
                        <StackPanel Margin="0,0,0,10">
                            <TextBlock Text="Magazyn:" Style="{StaticResource BaseTextBlockStyle}" Margin="0,0,0,5"/>
                            <ComboBox ItemsSource="{Binding AvailableWarehouses}"
                                      SelectedItem="{Binding EditedDeviceWarehouse, Mode=TwoWay}"
                                      DisplayMemberPath="WarehouseName"
                                      Style="{StaticResource {x:Type ComboBox}}"/>
                        </StackPanel>
                        <StackPanel Margin="0,0,0,10">
                            <TextBlock Text="Dodane przez:" Style="{StaticResource BaseTextBlockStyle}" Margin="0,0,0,5"/>
                            <ComboBox ItemsSource="{Binding AvailableUsers}"
                                      SelectedItem="{Binding EditedDeviceAddedByUser, Mode=TwoWay}"
                                      DisplayMemberPath="UserName"
                                      Style="{StaticResource {x:Type ComboBox}}"/>
                        </StackPanel>

                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,20">
                            <Button Content="Zapisz"
                                    Command="{Binding SaveDeviceCommand}"
                                    Style="{StaticResource {x:Type Button}}"
                                    Margin="0,0,10,0"/>
                            <Button Content="Anuluj"
                                    Command="{Binding CancelEditCommand}">
                                <Button.Template>
                                    <ControlTemplate TargetType="{x:Type Button}">
                                        <Border Background="#6C757D"
                                                BorderBrush="#6C757D"
                                                BorderThickness="{TemplateBinding BorderThickness}"
                                                CornerRadius="3">
                                            <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                                              VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                                              Margin="{TemplateBinding Padding}"
                                                              Content="{TemplateBinding Content}"/>
                                        </Border>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter Property="Background" Value="#5a6268"/>
                                                <Setter Property="BorderBrush" Value="#5a6268"/>
                                            </Trigger>
                                            <Trigger Property="IsPressed" Value="True">
                                                <Setter Property="Background" Value="#5a6268"/>
                                                <Setter Property="BorderBrush" Value="#5a6268"/>
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </Button.Template>
                            </Button>
                        </StackPanel>

                        <TextBlock Text="Akcje dla wybranego urządzenia" Style="{StaticResource SubTitleTextBlockStyle}" FontWeight="SemiBold" Margin="0,20,0,10"/>
                        <Separator Background="{StaticResource BorderBrush}" Height="1" Margin="0,0,0,15"/>

                        <StackPanel IsEnabled="{Binding IsDeviceSelected}">
                            <TextBlock Text="Przenieś urządzenie:" Style="{StaticResource BaseTextBlockStyle}" Margin="0,0,0,5"/>
                            <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                                <TextBlock Text="Do magazynu:" VerticalAlignment="Center" Margin="0,0,10,0" Style="{StaticResource BaseTextBlockStyle}"/>
                                <ComboBox ItemsSource="{Binding AvailableWarehouses}"
                                          SelectedItem="{Binding SelectedTargetWarehouse, Mode=TwoWay}"
                                          DisplayMemberPath="WarehouseName"
                                          Width="150"
                                          Style="{StaticResource {x:Type ComboBox}}"/>
                                <Button Content="Przenieś"
                                        Command="{Binding MoveDeviceToWarehouseCommand}"
                                        Style="{StaticResource {x:Type Button}}"
                                        Margin="10,0,0,0"/>
                            </StackPanel>
                            <StackPanel Orientation="Horizontal" Margin="0,0,0,20">
                                <TextBlock Text="Do użytkownika:" VerticalAlignment="Center" Margin="0,0,10,0" Style="{StaticResource BaseTextBlockStyle}"/>
                                <ComboBox ItemsSource="{Binding AvailableUsers}"
                                          SelectedItem="{Binding SelectedTargetUser, Mode=TwoWay}"
                                          DisplayMemberPath="UserName"
                                          Width="150"
                                          Style="{StaticResource {x:Type ComboBox}}"/>
                                <Button Content="Przenieś"
                                        Command="{Binding AssignDeviceToUserCommand}"
                                        Style="{StaticResource {x:Type Button}}"
                                        Margin="10,0,0,0"/>
                            </StackPanel>

                            <TextBlock Text="Dodaj wpis do historii napraw:" Style="{StaticResource BaseTextBlockStyle}" Margin="0,0,0,5"/>
                            <StackPanel Margin="0,0,0,10">
                                <TextBlock Text="Opis naprawy:" Style="{StaticResource BaseTextBlockStyle}" Margin="0,0,0,5"/>
                                <TextBox Text="{Binding NewRepairDescription, UpdateSourceTrigger=PropertyChanged}"
                                         AcceptsReturn="True" TextWrapping="Wrap" MinHeight="60"
                                         Style="{StaticResource {x:Type TextBox}}"/>
                            </StackPanel>
                            <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                                <TextBlock Text="Data rozpoczęcia:" VerticalAlignment="Center" Margin="0,0,10,0" Style="{StaticResource BaseTextBlockStyle}"/>
                                <DatePicker SelectedDate="{Binding NewRepairStartDate}"
                                            Width="140">
                                    <DatePicker.Template>
                                        <ControlTemplate TargetType="{x:Type DatePicker}">
                                            <Grid x:Name="PART_Root">
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="*"/>
                                                    <ColumnDefinition Width="Auto"/>
                                                </Grid.ColumnDefinitions>
                                                <DatePickerTextBox x:Name="PART_TextBox"
                                                         Grid.Column="0"
                                                         Background="Transparent"
                                                         Foreground="{StaticResource PrimaryForegroundBrush}"
                                                         BorderBrush="Transparent"
                                                         BorderThickness="0"
                                                         Padding="5"
                                                         CaretBrush="{StaticResource AccentBrush}"
                                                         VerticalContentAlignment="Center"
                                                         HorizontalContentAlignment="Left"/>
                                                <Button x:Name="PART_Button"
                                                        Grid.Column="1"
                                                        Style="{StaticResource {x:Type Button}}"
                                                        Width="30"
                                                        Margin="5,0,0,0"
                                                        Background="{StaticResource SecondaryBackgroundBrush}"
                                                        BorderThickness="0">
                                                    <Path Data="M 0 0 L 4 4 L 8 0 Z" Stretch="Fill" Fill="{StaticResource PrimaryForegroundBrush}" Width="8" Height="4" VerticalAlignment="Center" HorizontalAlignment="Center"/>
                                                </Button>
                                                <Popup x:Name="PART_Popup"
                                                       AllowsTransparency="True"
                                                       Placement="Bottom"
                                                       IsOpen="{TemplateBinding IsDropDownOpen}"
                                                       StaysOpen="False">
                                                    <Border BorderBrush="{StaticResource BorderBrush}"
                                                            BorderThickness="1"
                                                            Background="{StaticResource SecondaryBackgroundBrush}">
                                                        <Calendar x:Name="PART_Calendar"
                                                                  Foreground="{StaticResource PrimaryForegroundBrush}"
                                                                  Background="{StaticResource SecondaryBackgroundBrush}"
                                                                  BorderBrush="{StaticResource BorderBrush}"
                                                                  BorderThickness="0"/>
                                                    </Border>
                                                </Popup>
                                            </Grid>
                                        </ControlTemplate>
                                    </DatePicker.Template>
                                    <DatePicker.Resources>
                                        <Style TargetType="{x:Type Calendar}">
                                            <Setter Property="Background" Value="{StaticResource SecondaryBackgroundBrush}"/>
                                            <Setter Property="Foreground" Value="{StaticResource PrimaryForegroundBrush}"/>
                                            <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}"/>
                                            <Setter Property="BorderThickness" Value="1"/>
                                        </Style>
                                        <Style TargetType="{x:Type CalendarDayButton}">
                                            <Setter Property="Background" Value="Transparent"/>
                                            <Setter Property="Foreground" Value="{StaticResource PrimaryForegroundBrush}"/>
                                            <Setter Property="Template">
                                                <Setter.Value>
                                                    <ControlTemplate TargetType="{x:Type CalendarDayButton}">
                                                        <Border Background="{TemplateBinding Background}"
                                                                BorderBrush="{TemplateBinding BorderBrush}"
                                                                BorderThickness="{TemplateBinding BorderThickness}">
                                                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                                        </Border>
                                                        <ControlTemplate.Triggers>
                                                            <Trigger Property="IsMouseOver" Value="True">
                                                                <Setter Property="Background" Value="{StaticResource HoverBackgroundBrush}"/>
                                                            </Trigger>
                                                            <Trigger Property="IsSelected" Value="True">
                                                                <Setter Property="Background" Value="{StaticResource SelectedBackgroundBrush}"/>
                                                            </Trigger>
                                                        </ControlTemplate.Triggers>
                                                    </ControlTemplate>
                                                </Setter.Value>
                                            </Setter>
                                        </Style>
                                        <Style TargetType="{x:Type CalendarButton}">
                                            <Setter Property="Background" Value="{StaticResource SurfaceBackgroundBrush}"/>
                                            <Setter Property="Foreground" Value="{StaticResource PrimaryForegroundBrush}"/>
                                            <Setter Property="Template">
                                                <Setter.Value>
                                                    <ControlTemplate TargetType="{x:Type CalendarButton}">
                                                        <Border Background="{TemplateBinding Background}"
                                                                BorderBrush="{TemplateBinding BorderBrush}"
                                                                BorderThickness="{TemplateBinding BorderThickness}">
                                                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                                        </Border>
                                                        <ControlTemplate.Triggers>
                                                            <Trigger Property="IsMouseOver" Value="True">
                                                                <Setter Property="Background" Value="{StaticResource HoverBackgroundBrush}"/>
                                                            </Trigger>
                                                        </ControlTemplate.Triggers>
                                                    </ControlTemplate>
                                                </Setter.Value>
                                            </Setter>
                                        </Style>
                                    </DatePicker.Resources>
                                </DatePicker>
                            </StackPanel>
                            <StackPanel Orientation="Horizontal" Margin="0,0,0,20">
                                <TextBlock Text="Data zakończenia:" VerticalAlignment="Center" Margin="0,0,10,0" Style="{StaticResource BaseTextBlockStyle}"/>
                                <DatePicker SelectedDate="{Binding NewRepairEndDate}"
                                            Width="140">
                                    <DatePicker.Template>
                                        <ControlTemplate TargetType="{x:Type DatePicker}">
                                            <Grid x:Name="PART_Root">
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="*"/>
                                                    <ColumnDefinition Width="Auto"/>
                                                </Grid.ColumnDefinitions>
                                                <DatePickerTextBox x:Name="PART_TextBox"
                                                         Grid.Column="0"
                                                         Background="Transparent"
                                                         Foreground="{StaticResource PrimaryForegroundBrush}"
                                                         BorderBrush="Transparent"
                                                         BorderThickness="0"
                                                         Padding="5"
                                                         CaretBrush="{StaticResource AccentBrush}"
                                                         VerticalContentAlignment="Center"
                                                         HorizontalContentAlignment="Left"/>
                                                <Button x:Name="PART_Button"
                                                        Grid.Column="1"
                                                        Style="{StaticResource {x:Type Button}}"
                                                        Width="30"
                                                        Margin="5,0,0,0"
                                                        Background="{StaticResource SecondaryBackgroundBrush}"
                                                        BorderThickness="0">
                                                    <Path Data="M 0 0 L 4 4 L 8 0 Z" Stretch="Fill" Fill="{StaticResource PrimaryForegroundBrush}" Width="8" Height="4" VerticalAlignment="Center" HorizontalAlignment="Center"/>
                                                </Button>
                                                <Popup x:Name="PART_Popup"
                                                       AllowsTransparency="True"
                                                       Placement="Bottom"
                                                       IsOpen="{TemplateBinding IsDropDownOpen}"
                                                       StaysOpen="False">
                                                    <Border BorderBrush="{StaticResource BorderBrush}"
                                                            BorderThickness="1"
                                                            Background="{StaticResource SecondaryBackgroundBrush}">
                                                        <Calendar x:Name="PART_Calendar"
                                                                  Foreground="{StaticResource PrimaryForegroundBrush}"
                                                                  Background="{StaticResource SecondaryBackgroundBrush}"
                                                                  BorderBrush="{StaticResource BorderBrush}"
                                                                  BorderThickness="0"/>
                                                    </Border>
                                                </Popup>
                                            </Grid>
                                        </ControlTemplate>
                                    </DatePicker.Template>
                                    <DatePicker.Resources>
                                        <Style TargetType="{x:Type Calendar}">
                                            <Setter Property="Background" Value="{StaticResource SecondaryBackgroundBrush}"/>
                                            <Setter Property="Foreground" Value="{StaticResource PrimaryForegroundBrush}"/>
                                            <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}"/>
                                            <Setter Property="BorderThickness" Value="1"/>
                                        </Style>
                                        <Style TargetType="{x:Type CalendarDayButton}">
                                            <Setter Property="Background" Value="Transparent"/>
                                            <Setter Property="Foreground" Value="{StaticResource PrimaryForegroundBrush}"/>
                                            <Setter Property="Template">
                                                <Setter.Value>
                                                    <ControlTemplate TargetType="{x:Type CalendarDayButton}">
                                                        <Border Background="{TemplateBinding Background}"
                                                                BorderBrush="{TemplateBinding BorderBrush}"
                                                                BorderThickness="{TemplateBinding BorderThickness}">
                                                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                                        </Border>
                                                        <ControlTemplate.Triggers>
                                                            <Trigger Property="IsMouseOver" Value="True">
                                                                <Setter Property="Background" Value="{StaticResource HoverBackgroundBrush}"/>
                                                            </Trigger>
                                                            <Trigger Property="IsSelected" Value="True">
                                                                <Setter Property="Background" Value="{StaticResource SelectedBackgroundBrush}"/>
                                                            </Trigger>
                                                        </ControlTemplate.Triggers>
                                                    </ControlTemplate>
                                                </Setter.Value>
                                            </Setter>
                                        </Style>
                                        <Style TargetType="{x:Type CalendarButton}">
                                            <Setter Property="Background" Value="{StaticResource SurfaceBackgroundBrush}"/>
                                            <Setter Property="Foreground" Value="{StaticResource PrimaryForegroundBrush}"/>
                                            <Setter Property="Template">
                                                <Setter.Value>
                                                    <ControlTemplate TargetType="{x:Type CalendarButton}">
                                                        <Border Background="{TemplateBinding Background}"
                                                                BorderBrush="{TemplateBinding BorderBrush}"
                                                                BorderThickness="{TemplateBinding BorderThickness}">
                                                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                                        </Border>
                                                        <ControlTemplate.Triggers>
                                                            <Trigger Property="IsMouseOver" Value="True">
                                                                <Setter Property="Background" Value="{StaticResource HoverBackgroundBrush}"/>
                                                            </Trigger>
                                                        </ControlTemplate.Triggers>
                                                    </ControlTemplate>
                                                </Setter.Value>
                                            </Setter>
                                        </Style>
                                    </DatePicker.Resources>
                                </DatePicker>
                            </StackPanel>
                            <Button Content="Dodaj naprawę"
                                    Command="{Binding AddRepairHistoryCommand}"
                                    Style="{StaticResource {x:Type Button}}"/>
                        </StackPanel>
                    </StackPanel>
                </ScrollViewer>
            </Border>
        </Grid>
    </Grid>
</UserControl>