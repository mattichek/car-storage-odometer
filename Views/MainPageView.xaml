<Window x:Class="car_storage_odometer.Views.MainPageView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:car_storage_odometer.Views"
        mc:Ignorable="d"
        Title="MainPageView" Height="800" Width="1200"
        xmlns:prism="http://prismlibrary.com/">
    <Grid Background="#23272E">
        <Border Height="750" Width="1150" Background="#2C2F36" CornerRadius="10"
                HorizontalAlignment="Center" VerticalAlignment="Center" Padding="30">
            <Border.Effect>
                <DropShadowEffect BlurRadius="50"/>
            </Border.Effect>
            <DockPanel LastChildFill="True">
                <ContentControl prism:RegionManager.RegionName="TopBarRegion" DockPanel.Dock="Top"/>
                <ContentControl prism:RegionManager.RegionName="DashboardRegion" DockPanel.Dock="Bottom" Visibility="Hidden"/>
                <ContentControl prism:RegionManager.RegionName="LogsUsersRegion" DockPanel.Dock="Bottom"/>
                <ContentControl prism:RegionManager.RegionName="LogsDeviceRegion" DockPanel.Dock="Bottom"/>
                <ContentControl prism:RegionManager.RegionName="HostoryOfRepairRegion" DockPanel.Dock="Bottom"/>
                <ContentControl prism:RegionManager.RegionName="StorageRegion" DockPanel.Dock="Bottom"/>
                <ContentControl prism:RegionManager.RegionName="DevicesRegion" DockPanel.Dock="Bottom"/>
                <ContentControl prism:RegionManager.RegionName="AccountRegion" DockPanel.Dock="Bottom"/>
            </DockPanel>
        </Border>
    </Grid>
</Window>
